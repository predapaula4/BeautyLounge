create table ten (
    id       integer auto_increment,
    name     varchar(50),
    category varchar(50),
    quantity integer,
    texture  varchar(50),
    price    integer,
    primary key (id)
);

create table ochi (
    id       integer auto_increment,
    name     varchar(50),
    category varchar(50),
    quantity integer,
    texture  varchar(50),
    price    integer,
    primary key (id)
);

create table beauty_lounge (
    id integer auto_increment,
    name varchar(50),
    category varchar(50),
    quantity integer,
    texture varchar(50),
    price integer,
    primary key (id)
);

