server:
    port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:BeautyLounge
    username: bl
    password:
    generate-unique-name: false
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true